//default valores

/* const soma = ( a = 0, b = 0) => a + b;
 */

// shorthand Objects
const nome = "eduardo";
const lastName = 'Bazler';

const nomeCompleto = {
  nome, 
  lastName,
}

console.log(nomeCompleto);